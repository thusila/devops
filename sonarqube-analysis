node{
   stage('SCM Checkout'){
     git 'https://github.com/thusila/devops'
   }
  
   stage('SonarQube Analysis') {
        def mvnHome =  tool name: 'maven-3', type: 'maven'
        withSonarQubeEnv('sonar-6') { 
          sh "${mvnHome}/bin/mvn sonar:sonar"
        }
    }
   
   

}
